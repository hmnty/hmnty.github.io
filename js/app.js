$(document).ready(function(){

    // // select the buttons
    // buttons = $('.hero-slider .flickity-button');
    // dots = $('.hero-slider .flickity-page-dots');

    // // add them to a newly created div
    // slidernav = $('<div class="slider-navigation"></div>').append(buttons).append(dots)

    // // append the new div (my-div) to .main-carousel
    // $('.hero-slider').append(slidernav)
})
